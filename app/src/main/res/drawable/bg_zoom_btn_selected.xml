<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Semi-translucent dark mint green inner mask tint -->
    <solid android:color="#3313C884" />

    <!-- Ultra-sharp clean neon mint green accent line boundary -->
    <stroke
        android:width="1.5dp"
        android:color="@color/permission_green" />

    <!-- Perfect capsule shape curves logic mask -->
    <corners android:radius="@dimen/_50sdp" />
</shape>